package com.example.marvelcodetest.ui.component.toolbar

import androidx.lifecycle.AndroidViewModel
import com.example.marvelcodetest.core.MainApplication
import javax.inject.Inject

class ToolbarViewModel @Inject constructor(
    application: MainApplication): AndroidViewModel(application) {

}